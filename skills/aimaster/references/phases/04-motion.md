# Motion

Enter only after all active images are approved. For each shot, prepare a motion prompt covering camera, objects, characters, duration, and constraints. Bind every attempt to the selected `shot_id` as a motion revision only after its artifact exists inside the project; approve it only after technical QA passes. Keep visual QA independent and honest when no video-capable inspection ran.
